import {makeStyles} from "@material-ui/core/styles";

export default makeStyles({
  root: {
    height: '100%'
  },
  linearProgress: {
    width: '100%',
    position: 'absolute',
    top: 10,
    zIndex: 1202
  }
});
