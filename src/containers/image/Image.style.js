import {makeStyles} from "@material-ui/core/styles";

export default makeStyles({
  root: {
    // position: 'relative',
    // minHeight: '100%',
    margin: 10,
    padding: 10,
    boxSizing: "border-box",
    // border: '1px dashed'
  }
});
