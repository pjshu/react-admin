import {makeStyles} from "@material-ui/core";

export default makeStyles({
  typography: {
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    width: '100%',
    maxWidth: 660,
    textOverflow: 'ellipsis',
  },
  label: {
    fontWeight: 'bold'
  }
});
