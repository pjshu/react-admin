import {makeStyles} from "@material-ui/core";

export default makeStyles((theme) => ({
  container: {
    height: '100%'
  },
  paper: {
    width: 500,
    height: 400,
    padding: 40
  },
  fullWidth: {
    width: '100%',
  }
}));

