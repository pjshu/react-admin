import {makeStyles} from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    minHeight: '100%',
    position: 'relative',
    padding: 30,
    borderRadius: 4
  },
});


export default useStyles;
